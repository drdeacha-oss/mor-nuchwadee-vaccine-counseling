const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
document.getElementById('ageGrid').innerHTML=Object.keys(AGES).map(k=>`<button class="agebtn" onclick="showAge('${k}')">${k}</button>`).join('');document.getElementById('tableRows').innerHTML=TABLE.map(r=>`<tr><td><b>${r[0]}</b></td><td>${r[1]}</td><td>${r[2]}</td><td>${r[3]}</td></tr>`).join('');
function card(v,cls){return `<div class="vax ${cls}"><h3>${esc(v[0])}</h3><p>${esc(v[1])}</p><button class="more" onclick="showDetail('${v[2]}')">อ่านรายละเอียด</button></div>`}
function showAge(k){const a=AGES[k];document.getElementById('ageTitle').textContent='ลูกอายุ '+k;document.getElementById('routineList').innerHTML=a.routine.map(v=>card(v,'routine')).join('');document.getElementById('extraList').innerHTML=a.extra.length?a.extra.map(v=>card(v,'extra')).join(''):'<div class="small">ไม่มีวัคซีนเพิ่มเติมที่ต้องเน้นในช่วงนี้</div>';document.getElementById('annualBox').innerHTML=a.annual?`<div class="annual"><b>🌦️ วัคซีนประจำปี: ไข้หวัดใหญ่</b><div>ลูกได้รับวัคซีนไข้หวัดใหญ่ปีนี้แล้วหรือยัง?</div><button onclick="showDetail('flu')">อ่านรายละเอียด</button></div>`:'';document.getElementById('nextBox').innerHTML='<b>📅 ช่วงถัดไป:</b> '+esc(a.next);const r=document.getElementById('result');r.style.display='block';r.scrollIntoView({behavior:'smooth',block:'start'})}
function missed(flag){document.getElementById('missedMsg').style.display=flag?'block':'none'}
function showDetail(k){const d=DETAILS[k];if(!d)return;document.getElementById('modalBody').innerHTML=`<h2>${esc(d.title)}</h2><div class="info"><b>🦠 โรคนี้สำคัญอย่างไร</b>${esc(d.disease)}</div><div class="info"><b>🔄 ติดต่ออย่างไร</b>${esc(d.transmit)}</div><div class="info"><b>🛡️ วัคซีนช่วยอะไร</b>${esc(d.benefit)}</div><div class="info"><b>📅 ฉีดเมื่อไร / ใช้เมื่อไร</b>${esc(d.when)}</div><div class="info"><b>🌡️ อาการหลังฉีดที่พบบ่อย</b>${esc(d.common)}</div><div class="info"><b>⚠️ สิ่งที่ควรรู้</b>${esc(d.caution)}</div><button class="pricebtn" onclick="togglePrice()">💰 ดูราคา</button><div id="priceContent" class="pricecontent">${esc(d.price)}<div class="small">ราคาอาจเปลี่ยนแปลง กรุณายืนยันในวันที่รับบริการ</div></div><div class="review">ข้อมูลทางการแพทย์ทบทวนล่าสุด: สิงหาคม 2569</div>`;document.getElementById('modal').classList.add('show')}
function togglePrice(){const x=document.getElementById('priceContent');x.style.display=x.style.display==='block'?'none':'block'}function closeModal(){document.getElementById('modal').classList.remove('show')}function openPage(id){document.querySelectorAll('.page').forEach(x=>x.classList.remove('on'));document.getElementById(id).classList.add('on');window.scrollTo({top:0,behavior:'smooth'})}function home(){openPage('home')}
function searchVax(){const q=document.getElementById('searchInput').value.trim().toLowerCase(),box=document.getElementById('searchResults');if(!q){box.innerHTML='';return}const hits=Object.entries(DETAILS).filter(([k,d])=>(d.title+' '+d.disease+' '+d.benefit+' '+d.when).toLowerCase().includes(q)).slice(0,8);box.innerHTML=hits.length?hits.map(([k,d])=>`<div class="searchhit" onclick="showDetail('${k}')"><b>${esc(d.title)}</b><div class="small">${esc(d.benefit)}</div></div>`).join(''):'<div class="small">ไม่พบวัคซีนที่ตรงกับคำค้น</div>'}